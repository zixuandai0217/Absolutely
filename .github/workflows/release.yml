name: Release VSIX

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Validate theme
        run: npm run validate

      - name: Package VSIX
        run: npm run package

      - name: Resolve VSIX asset
        id: vsix
        run: |
          VERSION="$(node -p "require('./package.json').version")"
          ASSET="absolutely-theme-${VERSION}.vsix"
          test -f "$ASSET"
          echo "asset=$ASSET" >> "$GITHUB_OUTPUT"

      - name: Write release notes
        run: |
          {
            echo "Absolutely Theme ${GITHUB_REF_NAME}"
            echo
            echo "- VS Code-compatible Absolutely Light and Absolutely Dark themes."
            echo "- Codex App Absolutely source colors are locked by validation."
            echo "- VSIX package is attached below for local installation."
          } > release-notes.md

      - name: Publish GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "${GITHUB_REF_NAME}" "${{ steps.vsix.outputs.asset }}" \
            --repo "${GITHUB_REPOSITORY}" \
            --title "Absolutely Theme ${GITHUB_REF_NAME}" \
            --notes-file release-notes.md \
            --verify-tag
